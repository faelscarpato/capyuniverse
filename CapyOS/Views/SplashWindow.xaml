<Window x:Class="CapyOS.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" Width="420" Height="260" Background="#19000000" AllowsTransparency="True" ShowInTaskbar="False" Topmost="True">
    <Grid>
        <Border CornerRadius="12" Background="{DynamicResource App.SurfaceBrush}" Padding="24" RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.96" To="1" Duration="0:0:0.25"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.96" To="1" Duration="0:0:0.25"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="CapyOS" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource App.ForegroundBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Inicializando..." Margin="0,8,0,0" Foreground="{DynamicResource App.ForegroundBrush}" HorizontalAlignment="Center"/>
                <Grid Width="120" Height="120" Margin="0,16,0,0" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Grid.RenderTransform>
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:1.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                    <Ellipse Width="100" Height="100" Stroke="{DynamicResource AccentBrush}" StrokeThickness="6" Opacity="0.2"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
