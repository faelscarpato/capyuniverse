<UserControl x:Class="CapyOS.Views.CapyStoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource App.SurfaceBrush}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="CapyStore" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource App.ForegroundBrush}"/>
            <ComboBox Width="160" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Margin="8,0,0,0"/>
            <TextBox Width="220" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding AppsView}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" Height="180" Margin="8" CornerRadius="10" Background="{DynamicResource App.BackgroundBrush}" Padding="12">
                            <StackPanel>
                                <TextBlock Text="{Binding IconPath}" FontSize="24"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource App.ForegroundBrush}"/>
                                <TextBlock Text="{Binding Category}" Foreground="{DynamicResource App.ForegroundBrush}"/>
                                <TextBlock Text="{Binding AppType}" Foreground="{DynamicResource App.ForegroundBrush}"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource App.ForegroundBrush}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Executar" Command="{Binding DataContext.ExecuteAppCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                    <ToggleButton Content="Fixar" Margin="8,0,0,0" IsChecked="{Binding IsPinnedDock}" Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                    <ToggleButton Content="Favorito" Margin="8,0,0,0" IsChecked="{Binding IsFavorite}" Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
