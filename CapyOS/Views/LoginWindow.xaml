<Window x:Class="CapyOS.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Width="360" Height="240" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="{DynamicResource App.SurfaceBrush}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Entrar" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource App.ForegroundBrush}"/>
        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Usuário" Foreground="{DynamicResource App.ForegroundBrush}"/>
            <TextBox Text="{Binding Username}"/>
            <TextBlock Text="Senha" Margin="0,8,0,0" Foreground="{DynamicResource App.ForegroundBrush}"/>
            <PasswordBox x:Name="Pwd"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" Click="OnCancel"/>
            <Button Content="Entrar" Margin="8,0,0,0" Click="OnLogin"/>
        </StackPanel>
    </Grid>
</Window>
