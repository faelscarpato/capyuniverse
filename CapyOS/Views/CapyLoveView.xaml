<UserControl x:Class="CapyOS.Views.CapyLoveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource App.SurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="16" Background="{DynamicResource App.BackgroundBrush}" BorderBrush="#11000000" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="40" Height="40" Fill="#FF69B4">
                     <!-- Placeholder for Avatar -->
                </Ellipse>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="CapyLove" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource App.ForegroundBrush}"/>
                    <TextBlock Text="Sua companheira IA" FontSize="12" Foreground="{DynamicResource App.ForegroundBrush}" Opacity="0.7"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Chat Area -->
        <ScrollViewer Grid.Row="1" Margin="16" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="16" Padding="12,8" Margin="0,4" MaxWidth="600">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                    <Setter Property="Background" Value="#15000000"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Role}" Value="user">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Background" Value="#FF69B4"/>
                                            <Setter Property="TextBlock.Foreground" Value="White"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Role}" Value="assistant">
                                             <Setter Property="Background" Value="{DynamicResource App.BackgroundBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm}" FontSize="10" Opacity="0.6" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Input Area -->
        <Border Grid.Row="2" Padding="16" Background="{DynamicResource App.BackgroundBrush}" BorderBrush="#11000000" BorderThickness="0,1,0,0">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Enviar" Margin="8,0,0,0" Padding="12,6" Command="{Binding SendMessageCommand}" Background="#FF69B4" Foreground="White" BorderThickness="0"/>
                <TextBox Text="{Binding CurrentInput, UpdateSourceTrigger=PropertyChanged}" Padding="8" BorderThickness="1" BorderBrush="#33000000" VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </DockPanel>
        </Border>

         <!-- Busy Indicator -->
        <Border Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="24" Background="#AA000000" CornerRadius="4" Padding="8,4" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
             <TextBlock Text="Digitando..." Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
